# Package

version       = "0.1.0"
author        = "Mythical Forest Collective"
description   = "A JVM backend for Nim! Likely isn't (and never will be) perfect, but it should at least let core types work and allow for use of Nim syntax instead of Java!"
license       = "MIT"
srcDir        = "src"
bin           = @["njvm"]


# Dependencies

requires "nim >= 1.7.1"
